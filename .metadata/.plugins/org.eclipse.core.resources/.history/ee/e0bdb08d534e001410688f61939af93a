package raphaelpantaleao.katabanckocr.parser;

public abstract class AbstractNumberParser implements NumberParser {
	abstract public String parse(String digits);

	abstract protected String getPattern();

	protected String parse(String digits, int pos) {
		int i = pos * 3;
		boolean isZero = false;
		for (int j = 0; j < 4; j++) {
			isZero = digits.charAt(i + 27 * j) == getPattern().charAt(j * 3)
					&& digits.charAt(i + 1 + 27 * j) == getPattern().charAt(
							j * 3 + 1)
					&& digits.charAt(i + 2 + 27 * j) == getPattern().charAt(
							j * 3 + 2);
		}
		if (isZero) {
			if (i >= 24)
				return "0";
			else
				return "0" + parse(digits, ++pos);
		} else {
			return next(digits, pos);
		}
	}

	private String next(String digits, int pos) {
		return null;
	}
}
